name: qwen3-tts
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.8
  - pip
  - numpy
  - scipy
  - pip:
    - qwen-tts
    - torch>=2.0.0
    - soundfile>=0.12.0
    - flask>=2.0.0
    - flask-cors>=3.0.0